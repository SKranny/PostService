server:
  port: 8080
spring:
  datasource:
    password: Nikodim350z
    username: postgres
    url: jdbc:postgresql://localhost:5432/post-service
    driver-class-name: org.postgresql.Driver
  jpa:
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQL10Dialect
    hibernate:
      ddl-auto: update

